# npm lock file — このディレクトリはpnpmモノレポで管理されるため不要
# EASがnpmを誤検出する原因になるため除外する
package-lock.json

# ローカルモデルファイル（大容量・gitには不要）
*.gguf
*.bin
lambda/models/

# デバッグ用スクリーンショット
mac_*.png
ws*.png

# ローカル秘密情報（gitに入れない）
.env.local
