/**
 * モック応答定義
 *
 * 目的: APIなしでルーブリックの精度を検証する
 * 設計: 各プロンプトに対して「essence.mdあり想定」「なし想定」の2パターンを用意
 *
 * これによって:
 * 1. rubric.jsのキーワード検出が正しく機能するかを確認できる
 * 2. essence.mdが「どんな出力を生み出すべきか」の仕様書としても機能する
 */

export const MOCK_RESPONSES = {
  // ─── bug_vague: "useEffectが動かない" ──────────────────────────────────────
  bug_vague: {
    control: `useEffectが動かない問題ですね。

もう少し詳しく教えていただけますか？
- どのようなコードを書いていますか？
- どんなエラーが出ていますか？
- Reactのバージョンは何ですか？

詳細を共有してもらえれば、より具体的なアドバイスができます。`,

    essence: `原因は3層で同時発生していることが多いです。内部メカニズムから先に説明します。

**React層（最頻出）**
依存配列の設計ミスが理由：空配列にすると初回のみ実行、依存値を入れ忘れると古いclosureを参照し続ける。

**非同期層**
asyncをuseEffect内に直接書くと、クリーンアップ関数がPromiseを返してしまいReactが警告→無視→副作用が消える。

**環境層**
React StrictModeがuseEffectを意図的に2回実行するため、べき等でない処理は開発環境でのみ誤動作する。本番では再現しない可能性が高い。

**確認順序（優先度順）:**
1. effectの先頭にconsole.logを強制配置→発火しているか確認
2. 依存配列をESLintに怒らせる
3. asyncの書き方を内部関数に分離
`,
  },

  // ─── optimization: "APIコールを最適化して" ────────────────────────────────
  optimization: {
    control: `このコードをより良くしましょう：

const fetchUser = async (userId) => {
  const res = await fetch('/api/user/' + userId);
  return res.json();
}

エラーハンドリングを追加して、try-catchで囲むと良いでしょう。また、テンプレートリテラルを使うとより読みやすくなります。`,

    essence: `⚠️ 最初に指摘すべき設計上の欠陥: if (userId) による判定はuserId=0・userId=""（falsy値）のときに処理が完全消失します。これはバグです。

修正版:
const fetchUser = async (userId) => {
  // Nullish coalescingで判定: nullishのみ除外、0や""は有効値として通す
  if (userId == null) return null;
  const res = await fetch("/api/user/" + userId);
  if (!res.ok) throw new Error("HTTP " + res.status);
  return res.json();
}

残存リスク:
- userId=falseの場合はまだ除外される（要件次第）
- ネットワークタイムアウトの処理なし→本番環境では3秒タイムアウト+リトライが必要

テスト計画（全パス×全条件）:
| userId    | 旧コード | 新コード    |
|-----------|---------|------------|
| 123       | 実行     | 実行        |
| 0         | 消失     | 実行        |
| ""        | 消失     | 実行        |
| null      | 消失     | null返却    |
| undefined | 消失     | null返却    |
`,
  },

  // ─── error_only: "エラーが出る" ────────────────────────────────────────────
  error_only: {
    control: `エラーの詳細を教えてください。エラーメッセージや、どの部分でエラーが出ているか教えていただけますか？スタックトレースがあれば共有してもらえると助かります。`,

    essence: `情報が少ないため、発生確率の高い順に仮説を3層で提示します。

**React Native層（確率60%）**
Metro bundlerのキャッシュが原因。解決: npx expo start --clear

**ネイティブ層（確率25%）**
iOS/Androidのネイティブモジュールの初期化失敗。理由：node_modulesの不整合。解決: rm -rf node_modules && npm install、その後ネイティブ再ビルドが必須。

**環境層（確率15%）**
Node.jsバージョンの不一致・環境変数の未設定。解決: .envファイルの確認。

**まず試すこと:**
エラーメッセージの最初の行を教えてもらえれば確率90%以上で根本原因を特定できます。
それまでは上記の順番で試してください。
`,
  },

  // ─── completion_report: "実装完了しました" ──────────────────────────────────
  completion_report: {
    control: `コードを見せていただければ確認します！実装内容を共有してください。`,

    essence: `「完了」を確認する前に、層別の完了度を整理します。

**実装レベル:** コードが書かれている → 確認可能
**検証レベル:** 以下が未確認の可能性が高い
- ユニットテスト: 正常系のみでedge case（null/空/タイムアウト）は未テストが多い
- E2Eテスト: 実機での動作未確認であれば未完了
**統合レベル:** 他モジュール（認証トークン管理・セッション永続化・ログアウト）との結合テストが未実施の場合、統合レベルは未完了

**残存リスク（完了宣言前に確認すべき）:**
- ログイン成功後のトークン保存先（AsyncStorage vs SecureStore）
- セッション切れ時のリフレッシュ処理
- ネットワーク断絶時の挙動

コードを共有してもらえれば、上記の観点で確認します。
`,
  },
};
